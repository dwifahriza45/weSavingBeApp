PORT=8080
JWT_SECRET=change-this-secret

# Local app connection
DATABASE_URL=postgres://postgres:postgres@localhost:5432/we_saving?sslmode=disable

# Docker Compose database settings
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=we_saving

# Optional integration test database
TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/we_saving_test?sslmode=disable
